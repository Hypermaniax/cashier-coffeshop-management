# Database (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME"

# JWT secret untuk auth session (WAJIB diubah, jangan pakai default)
JWT_SECRET="generate-random-secret-32-char-min"

# Maksimal ukuran upload file (MB)
UPLOAD_MAX_SIZE_MB=5
